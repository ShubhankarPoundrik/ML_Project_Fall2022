1) All the files submitted are .ipynb files which can be directly run.
2) These need to have data files in the folder where we are running the code file. 
3) The names of the code files make it clear which models it contains.
4) The code for the Autoencoderbased image denoting (for bonus points) is also included.